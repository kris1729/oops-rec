####  cheak kre ki dono tree ek jaise hai kya

sabi ko ek dushre ke sabhi node ko compair krna


// sudo code 



bool cheak(root1, root2){

    if(r1&&r2)return 1 ;
    if(!r1&&r2 || r1&&!r1) return 0;

    if(r1->data != r2->data) return 0;
    return  cheak(r1->left, r2->left) && cheak(r1->right,r2->right);
   
}


time and space complexity O(n) and O(n)