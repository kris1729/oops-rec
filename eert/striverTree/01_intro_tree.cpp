// Hirical order Data structre / Subtree
// BT --> aT max two child of Each Node
// ansesters(Grandfater --> Father ---> Childrens)


// FULL TREE --> All Parent have Only two child.

// COMPLETE TREE --> All level completley field except last level && as Possible as Node is at left side

// PERFECT TREE --> All leaf should be at the same level

// BALANCED TREE --> Height at maxi Log(N) where N = Number of Nodes

// DEGENERATE TREE --> tree is in form of starte line like queue like linkList 
